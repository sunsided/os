[alias]
uefi         = "uefi-dev"
uefi-dev     = "build -p uefi-loader --target x86_64-unknown-uefi"
uefi-release = "build -p uefi-loader --release --target x86_64-unknown-uefi"

[target.x86_64-unknown-uefi]
rustflags = [
  "-C", "panic=abort"
]